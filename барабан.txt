<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Барабан призов</title>
    <script src="https://telegram.org/js/telegram-web-app.js"></script>
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            background: var(--tg-theme-bg-color);
            color: var(--tg-theme-text-color);
            padding: 20px;
        }
        .drum {
            font-size: 60px;
            margin: 30px 0;
        }
        button {
            background: var(--tg-theme-button-color);
            color: var(--tg-theme-button-text-color);
            border: none;
            padding: 15px 30px;
            font-size: 18px;
            border-radius: 10px;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <h1>🎰 Барабан призов</h1>
    <div class="drum" id="drum">?</div>
    <button id="spinBtn">Крутить</button>

    <script>
        const prizes = ["🍎", "🍒", "🍋", "🍊", "💰", "💎", "🎁", "⭐"];
        const drum = document.getElementById('drum');
        const spinBtn = document.getElementById('spinBtn');

        spinBtn.addEventListener('click', () => {
            spinBtn.disabled = true;
            let spins = 0;
            
            // Анимация вращения
            const spinInterval = setInterval(() => {
                spins++;
                drum.textContent = prizes[Math.floor(Math.random() * prizes.length)];
                
                if (spins >= 10) {
                    clearInterval(spinInterval);
                    spinBtn.disabled = false;
                    
                    // Финальный результат
                    const finalPrize = prizes[Math.floor(Math.random() * prizes.length)];
                    drum.textContent = finalPrize;
                    
                    // Отправка результата в бота
                    if (window.Telegram) {
                        Telegram.WebApp.sendData(JSON.stringify({ prize: finalPrize }));
                    }
                }
            }, 100);
        });

        // Инициализация WebApp
        Telegram.WebApp.ready();
    </script>
</body>
</html>